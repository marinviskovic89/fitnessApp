<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- General theme for the app -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary theme colors -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Background color -->
        <item name="android:windowBackground">@color/colorBackground</item>
    </style>

    <!-- Style for EditText components -->
    <style name="EditTextStyle">
        <item name="android:textColor">@android:color/black</item> <!-- Text color -->
        <item name="android:textSize">16sp</item> <!-- Text size -->
        <item name="android:paddingStart">16dp</item> <!-- Padding from left -->
        <item name="android:paddingEnd">16dp</item> <!-- Padding from right -->
        <item name="android:background">@android:color/white</item> <!-- White background for EditText -->
    </style>

    <!-- Style for Button components -->
    <style name="ButtonStyle">
        <item name="android:textColor">@android:color/white</item> <!-- Text color -->
        <item name="android:textSize">16sp</item> <!-- Text size -->
        <item name="android:paddingTop">12dp</item> <!-- Top padding -->
        <item name="android:paddingBottom">12dp</item> <!-- Bottom padding -->
        <item name="android:backgroundTint">@color/colorButtonBackground</item> <!-- Background color -->
    </style>

</resources>
